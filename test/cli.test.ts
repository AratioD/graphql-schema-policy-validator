import { describe, it, expect } from 'bun:test'

describe('Math Operations', () => {
  it('adds two numbers', () => {
    expect(1 + 1).toBe(2)
  })

  it('subtracts two numbers', () => {
    expect(5 - 3).toBe(2)
  })
})
